# Безопасность

Если вы нашли уязвимость, опишите её владельцу репозитория приватно (issue с меткой security на GitHub, если включено, или через контакт в профиле).

**Не публикуйте** токены ботов, ключи API, `JWT_SECRET` и содержимое `.env` в issues и в коде.
